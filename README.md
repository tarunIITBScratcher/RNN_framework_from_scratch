# RNN_framework_from_scratch
A recurrent neural network framework (SimpleRNN, LSTM, GRU) built from scratch with only NumPy — no autograd, no PyTorch/TensorFlow — with hand-derived BPTT, applied to many-to-one sentiment analysis.
